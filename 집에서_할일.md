# 집에서 할 일 — 순서대로 하나씩

꿈해몽 작업 시작할 때부터 지금까지 만든 것을 전부 모았습니다.
**위에서부터 순서대로** 하시면 됩니다. 한 단계 끝날 때마다 멈춰도 됩니다.

---

## 1단계 · 파이썬 파일 6개 바꾸기 (10분)

깃허브 `claude/ai-chatbot-blog-writing-wib1jx` 가지에서 받아서,
평소 파이썬 파일들이 있는 폴더에 **덮어쓰기** 합니다.

| 파일 이름 | 새것인가 | 무엇이 달라지나 |
| --- | --- | --- |
| `blog_publish_pipeline.py` | 덮어쓰기 | **광고 위치**, 워드프레스 카테고리 자동 선택 |
| `blog_content_pipeline.py` | 덮어쓰기 | 꿈해몽 전용 프롬프트, 지난 글 링크, 두 단계 글쓰기 연결 |
| `blog_tistory_browser_draft.py` | 덮어쓰기 | 대표이미지, 글 카테고리, 홈주제, 자동 공개 발행 |
| `article_v2.py` | **새 파일** | 두 단계 글쓰기 (두루뭉술한 글 막기) |
| `dream_links.py` | **새 파일** | 꿈해몽 지난 글 링크 |
| `wp_category.py` | **새 파일** | 워드프레스 카테고리 고르기 |

> `blog_tistory_browser_draft.py` 는 **사장님이 코덱스에서 고친 것**을 바탕으로
> 만들었습니다. 카카오 로그인 부분은 사장님 것 그대로 살아 있습니다.

**확인할 것 하나** — `blog_publish_pipeline.py` 도 코덱스에서 손보신 적이 있나요?
있다면 덮어쓰기 전에 저에게 그 파일을 보내주세요. 사장님 수정이 지워집니다.

---

## 2단계 · `.env` 에 일곱 줄 추가 (3분)

`.env` 파일을 메모장으로 열어서 **맨 아래에** 붙여넣습니다.

```
ARTICLE_V2_CATEGORIES=
DREAM_BLOG_URL=https://seaga4.tistory.com
DREAM_DONE_SHEET=6.완료꿈
DREAM_LINK_COUNT=3
WP_CATEGORY_AUTO=1
TISTORY_HOME_TOPIC=경제
TISTORY_AUTO_PUBLISH_CATEGORIES=
```

지금은 **두 줄이 비어 있는 채로 두세요.** 나중에 6단계, 7단계에서 채웁니다.
나머지 다섯 줄은 그대로 두시면 됩니다.

---

## 3단계 · Apps Script 파일 4개 바꾸기 (10분)

구글 시트 → **확장 프로그램** → **Apps Script**.
**새로 만드는 게 아니라, 이미 있는 파일 4개의 내용만 바꿉니다.**
파일 하나마다: 안의 내용 전부 지우기(Ctrl+A → Delete) → 새 내용 붙여넣기 → 저장(💾).

- [ ] `260816글쓰기` ← `appsscript/260816글쓰기.gs`
- [ ] `화면` ← `appsscript/화면.html`
- [ ] `finwiz` ← `appsscript/finwiz.gs`
- [ ] `finwiz화면` ← `appsscript/finwiz화면.html`

> `finwiz자료` 와 `Code.gs` 는 **손대지 않습니다.** 그대로 두세요.

다 저장했으면 시트를 **새로고침**(F5)하고, 위 메뉴에 `블로그 자동화` 와
finwiz 항목 4개가 그대로 보이는지만 봅니다.

---

## 4단계 · 티스토리 로그인 다시 하기 (5분)

브라우저 프로필이 오래되면 로그인이 풀립니다. 발행하다 멈추는 원인 1위입니다.

1. 파워셸에서 평소처럼 `python blog_tistory_browser_draft.py` 실행
2. 크롬 창이 뜨면 **직접 손으로** 카카오 로그인 (2단계 인증까지)
3. 로그인이 끝나고 글쓰기 화면이 나오면 그때부터는 자동으로 진행됩니다

이 로그인은 한 번 해두면 프로필에 저장돼서 다음부터는 안 물어봅니다.

---

## 5단계 · 시험 발행 딱 한 개 (10분)

아무 도메인이나 하나 골라서 **한 개만** 발행해 봅니다.
그리고 발행된 글을 실제로 열어서 **눈으로** 확인합니다.

- [ ] 표에 **선(테두리)** 이 보이는가
- [ ] 소제목·목록·표가 있어서 글이 **덩어리로 나뉘어** 보이는가
- [ ] 광고가 **글 맨 위가 아니라** 첫 소제목을 지나서 나오는가
- [ ] 광고가 **표나 목록 한가운데를 갈라놓지** 않는가
- [ ] 광고가 **3개** 보이는가 (중간쯤 하나, 맺음말 앞 하나 포함)
- [ ] 티스토리라면 **대표이미지**가 걸려 있고, 카테고리가 맞게 잡혔는가

**이번에 바뀐 광고 위치**

| | 예전 | 새것 |
| --- | --- | --- |
| 첫 번째 광고 | 두 번째 문단 바로 뒤 (본문의 4%) | 본문 **22% 쯤, 소제목 바로 앞** |
| 두 번째 광고 | 문단 개수의 절반 지점 | 본문 **55% 쯤, 소제목 바로 앞** |
| 세 번째 광고 | 맺음말 앞 | 맺음말 앞 (그대로) |

소제목 바로 앞이 광고 효율이 가장 좋습니다. 읽던 사람이 한 단락을 끝내고
다음 소제목으로 눈을 옮기는 길목이라 광고가 자연스럽게 시야에 들어옵니다.
반대로 예전 자리(글 맨 위)는 읽기도 전에 나오는 자리라 클릭도 안 나오고
애드센스도 싫어하는 배치였습니다.

목록·표 한가운데는 아예 후보에서 뺐고, 광고끼리 너무 붙으면 그 광고는
넣지 않습니다. 본문이 800자보다 짧으면 광고를 아예 넣지 않습니다.

---

## 6단계 · 꿈해몽 자동 발행 켜기 (5분)

5단계가 잘 됐으면 이제 꿈해몽을 공개 발행까지 자동으로 돌립니다.

`.env` 에서 이 줄을 찾아 **카테고리 이름을 적습니다.**

```
TISTORY_AUTO_PUBLISH_CATEGORIES=꿈해몽
```

> 구글시트에 있는 **카테고리 이름 그대로** 적어야 합니다.
> 비워두면 예전처럼 임시저장까지만 하고 멈춥니다.

그리고 꿈해몽 글 하나를 발행해서 확인합니다.

- [ ] 글이 **비공개가 아니라 공개**로 올라갔는가
- [ ] 본문 안에 `더 자세히 보기 → ○○○ 바로가기` 링크가 **2~3개** 들어갔는가
- [ ] 그 링크를 눌렀을 때 **내가 예전에 쓴 꿈 글**로 가는가

---

## 7단계 · 두 단계 글쓰기 켜기 (5분)

두루뭉술한 글 문제를 고치는 것입니다. **한 카테고리로만 먼저 시험**하세요.

`.env` 에서:

```
ARTICLE_V2_CATEGORIES=복지
```

`복지` 자리에 시험해 볼 카테고리 이름 하나를 적습니다 (시트에 있는 이름 그대로).
쉼표로 여러 개도 됩니다. **비워두면 예전 방식 그대로** 돌아갑니다.

글을 한 개 뽑아서, 예전 글과 비교합니다.

- [ ] 「보통 ~한 편입니다」 「경우에 따라 다릅니다」 같은 **얼버무리는 말이 줄었는가**
- [ ] **구체적인 숫자와 기관 이름**이 들어갔는가
- [ ] 사람이 진짜 궁금해할 질문에 **답이 나와 있는가**

좋으면 카테고리를 하나씩 늘려가시면 됩니다. 나쁘면 그 줄을 다시 비우면 원상복구입니다.

---

## 8단계 · finwiz 글쓰기 (20분)

가장 중요한 블로그입니다. 순서가 조금 다릅니다.

1. 시트 메뉴에서 **finwiz 로드맵 가져오기** — 한 번만 하면 됩니다. 매번 안 해도 됩니다
2. **finwiz 사실카드** 열기 → 빈칸이 있으면 채우기
3. **finwiz 글 만들기** → 다음 번호 글이 만들어집니다
4. 만들어진 글에 **검사 결과**가 같이 나옵니다

**검사 결과 읽는 법**

| 색 | 뜻 | 할 일 |
| --- | --- | --- |
| 🔴 **막힘** | 금지 표현이나 틀린 숫자가 있음 | 그 문장을 고쳐야 함 |
| 🟡 **경고** | 표가 없다, 문단이 너무 길다 등 | 고치면 좋지만 발행해도 됨 |
| 🟢 없음 | 통과 | 바로 발행 |

전에 시트에서 **검사실패 → 발행대기**로 손수 바꾸신 줄들이 있는데,
그 줄들은 그대로 발행하시면 됩니다. 이번에 **정상 문장을 막던 규칙 5개를
고쳤기 때문에** 다시 만들면 대부분 통과합니다.

- [ ] 검사실패로 남아 있는 줄이 있으면 **finwiz 글 다시 만들기**로 새로 뽑아보기
- [ ] 그래도 막히면 어떤 문구 때문인지 저에게 보내주세요

---

## 9단계 · 저에게 보내주실 것 (2분)

이것만 알려주시면 남은 것 두 개를 마무리합니다.

1. **홈주제 로그 한 줄** — 티스토리 발행할 때 파워셸에 이런 줄이 뜹니다.
   ```
   목록에 보인 것: [...]
   ```
   그 줄을 그대로 복사해서 보내주세요. 홈주제 자동선택을 끝낼 수 있습니다.

2. **`blog_publish_pipeline.py` 를 코덱스에서 고치신 적이 있는지** (1단계 참고)

---

## 안 되면 이것부터

| 증상 | 해결 |
| --- | --- |
| 시트에서 `PERMISSION_DENIED` | 크롬 **시크릿창**(InPrivate)으로 시트를 열거나, 구글 계정 하나만 남기고 로그아웃 |
| 파워셸에서 창이 바로 닫힘 | 4단계 로그인부터 다시 |
| 화면이 `about:blank` 에서 멈춤 | 크롬 창을 직접 닫고 다시 실행 |
| 광고가 안 보임 | `ads_config.json` 에 그 도메인이 있는지, 광고 코드 파일이 비어 있지 않은지 |
| 표에 선이 없음 | 3단계 `260816글쓰기` 를 붙여넣고 저장했는지 |
